[project]
name = "dbt-project"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Akash Kumar Jha", email = "akashjhaucet@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "dbt-core>=1.12.4",
]

[project.scripts]
dbt-project = "dbt_project:main"

[build-system]
requires = ["uv_build>=0.12.13,<0.13.0"]
build-backend = "uv_build"
